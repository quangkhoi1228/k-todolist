---
description: Dùng browser thật cho automation Teams/Zalo (sync) — không bắt buộc verify UI qua Chrome
alwaysApply: true
---

# Dùng Browser Thật cho Automation Teams/Zalo

Dự án này tương tác với **Microsoft Teams** và **Zalo** qua trình duyệt — cả hai đều chặn browser automation (Playwright headless, Puppeteer, v.v.).

## Nguyên tắc

1. **Sync Teams/Zalo phải dùng browser thật**: Các sync script PHẢI dùng Playwright với Chrome profile thật (`useRealChrome: true`, `createStealthContext(config)`) — không dùng chromium headless mặc định (dễ bị phát hiện là bot).

2. **Verify UI qua Chrome KHÔNG bắt buộc**: Không tự ý mở browser/profile để verify UI trừ khi user yêu cầu rõ. Chỉ mở browser khi user chủ động bảo verify UI (xem `screenshot-verify.mdc`).

## Các file liên quan

- `agents/pm/lib/teams-automator.ts` — core Teams extraction (dùng stealth context)
- `agents/pm/lib/zalo-automator.ts` — core Zalo extraction
- Scripts trong `agents/pm/scripts/` — các sync và debug scripts
- Script test: `final_test.ts`, `debug_*.ts`, `check_*.ts`

## Khi debug / chạy sync

```bash
# Dùng Playwright với user Chrome profile thật khi chạy sync
HEADLESS=false npx tsx agents/pm/scripts/sync-all-projects.ts
HEADLESS=false USER_ID=xxx PROJECT_ID=xxx CHAT_NAME="..." npx tsx agents/pm/scripts/sync-single-chat.ts
```